services:
  aetheria:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aetheria-simulation
    ports:
      - '8080:80'
    restart: unless-stopped
    environment:
      - NODE_ENV=production
